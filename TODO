Re-run cppcheck, use Coverity Scan

Check when is appropriate to replace gsl_rng_uniform() with gsl_rng_uniform_pos()

There is a memory leak in njsummat/njformul.c. This is not serious, but in a typical run a ~25MB per minute are leaked.

Variable-size grid!

